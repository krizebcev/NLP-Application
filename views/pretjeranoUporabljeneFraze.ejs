<!DOCTYPE html>
<html lang="en">
  <head>
    <% include partials/head %>
  </head>
  <body>
    <% include partials/menu %>
    <div class="container" id="textInputForm">
      <form action="/pretjeranoUporabljeneFraze" method="get">
        <div class="form-group">
          <label for="inputTextarea"
            >Ovdje unesite tekst nad kojim želite izvršiti analizu:</label
          >
          <textarea
            class="form-control"
            id="inputTextarea"
            rows="5"
            name="inputTextarea"
          >
<%= inputText %></textarea
          >
        </div>
        <div class="form-group">
          <button type="submit" class="btn btn-primary btn-lg">
            Otkrij pretjerano uporabljene fraze
          </button>
        </div>
      </form>
    </div>
    <% if (rezultat.length > 0) { %>
    <div class="container">
      <div class="alert alert-warning" role="alert" id="alertMessage">
        <h4 class="alert-heading">Upozorenje!</h4>
        <p>
          <i>
            Pretjerano uporabljene fraze su one koje se pojavljuju više od dva
            puta unutar jedne rečenice te ih se preporučuje zamijeniti s nekom
            od ponuđenih sličnih fraza.</i
          >
        </p>
        <hr />
        <% rezultat.forEach( recenica => { %>
        <p><%= recenica %></p>
        <hr />
        <% }); %>
      </div>
    </div>
    <% } %> <% if (rezultat.length == 0 && obavljenZahtjev == true) { %>
    <div class="container">
      <div class="alert alert-success" role="alert">
        <h4 class="alert-heading">Odlično!</h4>
        <p>
          U tekstu kojeg ste upisali nema niti jedna fraza koja se pojavljuje
          više od dva puta unutar jedne rečenice.
        </p>
      </div>
    </div>
    <% } %><% include partials/footer %>
  </body>
  <% include partials/script %>
</html>
